# テストシナリオ: 共通UI実装（SiteNavBar / App Header / Footer / TermsModal）

> 作成日: 2026-04-04
> 対象機能: SiteNavBar（サービス横断ナビゲーション）、App Header 調整（sticky top-8）、フッター再設計、TermsModal
> 対象ページ: `/`（http://localhost:5173）
> 前提条件:
> - `npm run dev` で開発サーバーが http://localhost:5173 で起動していること
> - FFmpeg.wasm がロード完了してメインUIが表示されていること（ローディング画面を通過済み）
> - ブラウザはデスクトップ Chrome を基準とする（レスポンシブ確認は各シナリオに明記）

---

## 正常系

| # | シナリオ | ページ | 操作手順 | 期待結果 |
|---|---------|--------|---------|---------|
| 1 | SiteNavBar が最上部に表示される | `/` | 1. `http://localhost:5173` にアクセスする 2. ページ最上部を確認する | 高さ `h-8`（32px）の濃紺（`bg-slate-800`）バーがページ最上部に表示される |
| 2 | SiteNavBar に関連サービスリンクが表示される | `/` | 1. ページ最上部の SiteNavBar を確認する | 「🎨」「🖼️」「🔊」「🎮」「🏆」の絵文字リンクが左から順に表示される |
| 3 | SiteNavBar の言語切り替えボタンが表示される | `/` | 1. SiteNavBar 右端を確認する | 「JA」「\|」「EN」が右端に横並びで表示される。初期状態では JA ボタンが白背景・太字のアクティブ状態になっている |
| 4 | SiteNavBar にスタジオロゴが表示される | `/` | 1. SiteNavBar 右端の言語ボタンの右を確認する | 丸型のスタジオロゴ画像（`studio-logo.png`）が表示される |
| 5 | SiteNavBar のリンクが外部 URL に遷移する | `/` | 1. SiteNavBar の「🖼️」リンクをクリックする | 新しいタブで `https://image-converter.yurinchi2525.com/` が開く。元のタブの URL は変化しない |
| 6 | スタジオロゴリンクが外部 URL に遷移する | `/` | 1. SiteNavBar のスタジオロゴをクリックする | 新しいタブで `https://yurinchi2525.com/` が開く |
| 7 | App Header が SiteNavBar の直下に固定される | `/` | 1. ページを下にスクロールして300px程度スクロールする 2. ページ上部を確認する | SiteNavBar（高さ 32px）の直下にアプリヘッダーが固定表示され、コンテンツの下に隠れない。ヘッダー背景は半透明のすりガラス効果で後ろのコンテンツが薄く透けて見える |
| 8 | App Header にサービス名が表示される | `/` | 1. App Header の内容を確認する | 「Easy Sound Converter」のサービス名と音符アイコンがヘッダー左側に表示される |
| 9 | App Header に LanguageSwitcher が表示されない | `/` | 1. App Header の内容を確認する | LanguageSwitcher（JA/EN ボタン）はヘッダー内に存在せず、SiteNavBar 内にのみ存在する |
| 10 | フッターに著作権とバージョンが表示される | `/` | 1. ページ最下部のフッターを確認する | フッター左側に「© Yu-Rin-Chi Game Studio 2026」と「v{バージョン番号}」が中点（·）区切りで表示される |
| 11 | フッターに X リンクが表示される | `/` | 1. フッター右側のリンク群を確認する | X ロゴ SVG と連絡先テキストが表示される。クリックで新しいタブが開く |
| 12 | フッターに GitHub リンクが表示される | `/` | 1. フッター右側のリンク群を確認する | GitHub ロゴ SVG と「GitHub」テキストが表示される。クリックで新しいタブが開く |
| 13 | フッターのシェアボタンをクリックするとドロップダウンが開く | `/` | 1. フッター右側の「シェア」ボタンをクリックする | ドロップダウンメニューが上方向に展開し「X でシェア」と「URL をコピー」の 2 項目が表示される |
| 14 | フッターの「X でシェア」リンクが正しい URL で遷移する | `/` | 1. フッターの「シェア」ボタンをクリックする 2. ドロップダウン内の「X でシェア」をクリックする | 新しいタブで `https://twitter.com/intent/tweet?url=...&text=...` が開く。ドロップダウンが閉じる |
| 15 | フッターの「URL をコピー」をクリックするとコピー成功表示が出る | `/` | 1. フッターの「シェア」ボタンをクリックする 2. ドロップダウン内の「URL をコピー」をクリックする | チェックアイコンと「コピーしました」テキストに切り替わる。約 1.5 秒後にドロップダウンが自動で閉じる |
| 16 | フッターの「利用規約」ボタンをクリックするとモーダルが開く | `/` | 1. フッター右側の「利用規約」ボタンをクリックする | TermsModal が画面中央に表示され、背景が半透明の黒オーバーレイで覆われる |
| 17 | TermsModal にタイトルと本文が表示される | `/` | 1. フッターの「利用規約」ボタンをクリックする | モーダルヘッダーに「利用規約」が表示される。本文エリアに「1. 本ツールについて」「2. 免責事項」「3. 著作権・ライセンス」「4. 規約の変更」の 4 セクションが表示される |
| 18 | TermsModal の閉じるボタンでモーダルが閉じる | `/` | 1. フッターの「利用規約」ボタンをクリックする 2. モーダルヘッダーの ✕ ボタンをクリックする | モーダルが閉じ、フッターが再び操作できる状態に戻る |
| 19 | JA → EN 言語切り替えで SiteNavBar のリンクテキストが英語になる | `/` | 1. SiteNavBar の「EN」ボタンをクリックする 2. SiteNavBar のリンクテキストを確認する | 各リンクのテキストが英語表記（「Free UI Assets」「Image Converter」「Sound Editor」等）に切り替わる |
| 20 | EN → JA 言語切り替えで SiteNavBar のリンクテキストが日本語に戻る | `/` | 1. SiteNavBar の「EN」ボタンをクリックする 2. 続けて「JA」ボタンをクリックする | 各リンクのテキストが日本語表記に戻る |
| 21 | EN 切り替え後に利用規約モーダルの本文が英語になる | `/` | 1. SiteNavBar の「EN」ボタンをクリックする 2. フッターの「Terms of Use」ボタンをクリックする | モーダルタイトルが「Terms of Use」になり、各セクション見出し・本文が英語で表示される |
| 22 | 言語設定がリロード後も維持される | `/` | 1. SiteNavBar の「EN」ボタンをクリックする 2. ブラウザをリロードする（F5 または Ctrl+R） 3. SiteNavBar の言語ボタン状態を確認する | 「EN」ボタンがアクティブ状態（白背景・太字）のままになっており、リンクテキストが英語のまま表示される |

---

## 異常系

| # | シナリオ | ページ | 操作手順 | 期待結果 |
|---|---------|--------|---------|---------|
| 23 | `studio-logo.png` が未配置のとき代替テキストが表示される | `/` | 1. `public/studio-logo.png` を意図的にリネームした状態でアクセスする | img 要素の alt テキスト「Yu-Rin-Chi Game Studio」が表示される（ブラウザの壊れた画像アイコンが出ても SiteNavBar レイアウトが崩れない） |
| 24 | シェアドロップダウン外をクリックするとドロップダウンが閉じる | `/` | 1. フッターの「シェア」ボタンをクリックしてドロップダウンを開く 2. ドロップダウン以外の領域（例: フッター左の著作権テキスト部分）をクリックする | ドロップダウンが閉じる |
| 25 | TermsModal の背景オーバーレイをクリックするとモーダルが閉じる | `/` | 1. フッターの「利用規約」ボタンをクリックしてモーダルを開く 2. モーダル外の暗い背景オーバーレイ部分をクリックする | モーダルが閉じる。メインコンテンツが再び操作できる状態に戻る |
| 26 | TermsModal 内（本文エリア）をクリックしてもモーダルが閉じない | `/` | 1. フッターの「利用規約」ボタンをクリックしてモーダルを開く 2. モーダル本文テキスト上でクリックする | モーダルが閉じず、表示が維持される |

---

## 境界値

| # | シナリオ | 条件 | 期待結果 |
|---|---------|------|---------|
| 27 | SiteNavBar の Z-index が App Header より上 | スクロール中にコンテンツが SiteNavBar / App Header の重なり部分を通過する | SiteNavBar（z-20）がコンテンツの上に重なって表示される。App Header（z-10）は SiteNavBar の下に隠れるが、SiteNavBar の下端からは正しく表示される |
| 28 | TermsModal が長い規約テキストでスクロール可能 | TermsModal の本文が画面高さを超えるとき（`max-h-[80vh]` を超える量のテキスト） | モーダル本体が縦スクロール可能になる。モーダルヘッダーの「利用規約」と閉じるボタンは常に最上部に固定（sticky）表示される |
| 29 | SiteNavBar リンクが 5 件以上あるとき | `SITE_LINK_GROUPS` に合計 5 件のリンクが定義されている（デフォルト設定） | 全リンクが SiteNavBar に収まって表示される。バーの高さ（h-8）が変化しない |
| 30 | コピー完了フィードバックが 1.5 秒後に自動消去される | URL コピー実行直後から 1.5 秒経過 | 「コピーしました」表示から元の「URL をコピー」表示に自動で戻り、ドロップダウンが閉じる |

---

## Web 固有チェック

| # | 確認項目 | ページ | 期待結果 |
|---|---------|--------|---------|
| 31 | レスポンシブ表示（モバイル 375px）SiteNavBar | `/` | ウィンドウ幅を 375px にすると、SiteNavBar のリンクテキスト（日本語/英語）が非表示になり絵文字のみ表示される。バーの高さ・ロゴ・言語ボタンは正常に表示される |
| 32 | レスポンシブ表示（タブレット 768px）SiteNavBar | `/` | ウィンドウ幅を 768px にすると、SiteNavBar のリンクテキストが表示される（`sm:inline` = 640px 以上で表示）。言語ボタンとロゴが右端に配置される |
| 33 | レスポンシブ表示（デスクトップ 1024px）全体レイアウト | `/` | ウィンドウ幅を 1024px にすると、SiteNavBar・App Header・フッターが正しくレイアウトされる。各要素が水平方向に収まり、縦崩れが発生しない |
| 34 | レスポンシブ表示（ワイド 1440px）全体レイアウト | `/` | ウィンドウ幅を 1440px にすると、SiteNavBar・App Header・フッターのコンテンツ幅が適切に制限される（`max-w-5xl` = 1024px 以内）。両端に余白が生まれる |
| 35 | キーボードナビゲーション: SiteNavBar リンク | `/` | Tab キーで SiteNavBar の各リンク（絵文字リンク×5、JA ボタン、EN ボタン、スタジオロゴ）に順番にフォーカスが当たる。フォーカスリングが視覚的に確認できる |
| 36 | キーボードナビゲーション: SiteNavBar 言語切り替え | `/` | Tab キーで JA/EN ボタンにフォーカスして Enter キーを押すと言語が切り替わる |
| 37 | キーボードナビゲーション: 利用規約モーダルを ESC で閉じる | `/` | 1. フッターの「利用規約」ボタンをクリックしてモーダルを開く 2. Escape キーを押す | モーダルが閉じる |
| 38 | キーボードナビゲーション: フッターリンク群 | `/` | Tab キーでフッターの X リンク・GitHub リンク・シェアボタン・利用規約ボタンに順番にフォーカスが当たる |
| 39 | ページ固定スクロール: SiteNavBar と App Header が追従する | `/` | 1. ページ最下部までスクロールする 2. ページ上部を確認する | SiteNavBar が画面最上部に固定表示されている。その直下（上から 32px の位置）に App Header が固定表示されている |
| 40 | URL 直接アクセス | `http://localhost:5173/` | ブラウザのアドレスバーに `http://localhost:5173/` を直接入力して Enter を押す | FFmpeg ローディング画面が表示された後、SiteNavBar・App Header・メインコンテンツ・フッターを含む完全なページが表示される |
| 41 | ブラウザの戻る/進む操作 | `/` | 1. SiteNavBar の外部リンクをクリックする（新タブが開く） 2. 元タブでブラウザの戻るボタンを押す | 元タブの状態（ファイル一覧、選択ファイル等）が維持されたまま同じページが表示される |
| 42 | シェアドロップダウン: `aria-expanded` 属性の切り替え | `/` | 1. フッターの「シェア」ボタンをクリックする | ボタンの `aria-expanded` 属性が `false` から `true` に変わる。閉じると再び `false` に戻る |
| 43 | TermsModal: `role="dialog"` と `aria-modal="true"` の確認 | `/` | 1. フッターの「利用規約」ボタンをクリックする | モーダル要素に `role="dialog"` および `aria-modal="true"` 属性が付与されている。`aria-labelledby` でタイトル（terms-title）が関連付けられている |
| 44 | 言語ボタン: `aria-pressed` 属性の切り替え | `/` | 1. SiteNavBar の「EN」ボタンをクリックする | EN ボタンの `aria-pressed` が `true` になり、JA ボタンの `aria-pressed` が `false` になる |
| 45 | SiteNavBar のリンク: `rel="noopener noreferrer"` の確認 | `/` | 1. SiteNavBar の外部リンクの DOM 属性を確認する | 各外部リンクに `target="_blank"` `rel="noopener noreferrer"` が設定されている |

---

## UX チェック

| # | 確認項目 | ページ | 期待結果 |
|---|---------|--------|---------|
| 46 | SiteNavBar リンクのホバー時に不透明度が変化する | `/` | SiteNavBar の絵文字リンクにマウスをホバーする | 不透明度が `opacity-60` から `opacity-100` にアニメーション遷移する（`transition-opacity duration-150`） |
| 47 | 言語切り替えボタンのホバー時にテキスト色が変化する | `/` | SiteNavBar の非アクティブな言語ボタン（例: EN ボタン）にマウスをホバーする | テキスト色が `text-slate-400` から `text-white` に変化する |
| 48 | スタジオロゴのホバー時に不透明度が変化する | `/` | SiteNavBar のスタジオロゴにマウスをホバーする | 不透明度が `opacity-80` から `opacity-100` にアニメーション遷移する |
| 49 | フッターのリンク/ボタンのホバー時にテキスト色が変化する | `/` | フッターの X リンク・GitHub リンク・シェアボタン・利用規約ボタンのいずれかにマウスをホバーする | ホバーしたリンク/ボタンのテキスト色が薄いグレーから濃いグレー（ライトモード）または明るい白（ダークモード）に変化する |
| 50 | シェアドロップダウンのメニュー項目ホバー時に背景色が変化する | `/` | 1. フッターの「シェア」ボタンをクリックする 2. ドロップダウン内の「X でシェア」または「URL をコピー」にマウスをホバーする | ホバーした項目の背景色が変化する（`hover:bg-slate-100`）。前後の項目の色は変化しない |
| 51 | TermsModal 表示中にページ背景がスクロールしない | `/` | 1. フッターの「利用規約」ボタンをクリックしてモーダルを開く 2. モーダル外の背景オーバーレイ上でマウスホイールをスクロールする | ページ背景コンテンツがスクロールされない（モーダル内のみスクロール可能） |
| 52 | TermsModal の閉じるボタン（✕）のホバー時に色が変化する | `/` | 1. 利用規約モーダルを開く 2. ✕ ボタンにマウスをホバーする | ✕ アイコンの色が `text-slate-400` から `text-slate-600`（ライトモード）に変化する |
| 53 | App Header と SiteNavBar 間の視覚的な階層が明確である | `/` | 1. ページを開いてヘッダー周辺を確認する | SiteNavBar（`bg-slate-800`、h-8）と App Header（`bg-gray-900/80` または `bg-white/80`、h-14）が明確に異なるスタイルで視覚的に区別できる。二つのバーが連続して見えてもそれぞれの領域が判別できる |
| 54 | フッターが常にページ最下部に位置する | `/` | 1. ファイルを追加せずにページを開く（コンテンツが少ない状態） | フッターがビューポートの最下部に表示される（コンテンツが少なくても中途半端な位置に浮かない。`mt-auto` または `min-h-screen` のレイアウト制御が効いている） |
